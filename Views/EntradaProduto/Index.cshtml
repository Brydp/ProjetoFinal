@model IEnumerable<ProjetoFinal.Models.EntradaProduto>

@{
    ViewData["Title"] = "Index";
}

<h1></h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProdutoId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataEntrada)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QuantidadeEntradaId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProdutoId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataEntrada)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QuantidadeEntradaId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EntradaProdutoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EntradaProdutoId">Details</a> |
            </td>
        </tr>
}
    </tbody>
</table>
